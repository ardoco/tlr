package edu.kit.kastel.mcse.ardoco.tlr.models.connectors.generators.antlr.elements;

/**
 * Represents all types of structural elements that can be found in the code.
 */
public enum Type {
    INTERFACE,
    CLASS,
    FUNCTION,
    FILE,
    PACKAGE,
    COMPILATIONUNIT,
    MODULE,
    NAMESPACE,
    VARIABLE
}